apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: protools-flowable
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - protools-flowable-demo
  rules:
    - host: protools-flowable-demo
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: protools-flowable
                port:
                  number: 8080
