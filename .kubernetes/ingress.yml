apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: protools-flowable-demo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - protools-flowable-demo.dev.insee.io
  rules:
    - host: protools-flowable-demo.dev.insee.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: protools-flowable-demo
                port:
                  number: 8080
